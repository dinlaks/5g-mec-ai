# Agent — ContentIntelligenceAgent
# 5G MEC Content Pre-positioning & Adaptive Streaming

# ── Agent framework ───────────────────────────────────────────────────────────
langgraph==0.2.45
langchain-core==0.3.15

# ── LLM API (LlamaStack → vLLM) ──────────────────────────────────────────────
llama-stack-client==0.4.2

# ── Observability (Langfuse) ──────────────────────────────────────────────────
langfuse==2.36.0

# ── MCP client (calls all 7 MCP servers) ─────────────────────────────────────
fastmcp==3.0.2

# ── Kafka (demand.predictions consumer) ──────────────────────────────────────
confluent-kafka==2.5.3

# ── API server (health, state, resume endpoints) ──────────────────────────────
fastapi==0.115.4
uvicorn[standard]==0.32.0

# ── Utilities ─────────────────────────────────────────────────────────────────
httpx==0.27.2
pyyaml==6.0.2
python-dotenv==1.0.1
tenacity==9.0.0       # retry logic for MCP tool calls
